/*Create an object with key-value pairs and use a loop to log each key and its corresponding value to the 
console. */

let obj = {
    Name:"Sakshi",
    Number:[1,2,3],
    Color:"Pink"
}

for(let i =0; i < obj.length; i++){
    console.log(obj[i])
}
